import { Tabs, Callout } from 'nextra/components'

# Troubleshooting Overview

Welcome to the troubleshooting guide for nextra-docs-starter-plus. This section provides solutions for the most common issues you may encounter.

## Quick Troubleshooting Checklist

Before diving into specific issues, try these common fixes:

1. **Restart your development server** - Many issues are resolved by simply restarting
2. **Clear cache** - Remove `.next` folder and rebuild
3. **Check environment variables** - Ensure all required variables are set
4. **Verify dependencies** - Run `npm install` or `pnpm install`
5. **Check browser console** - Look for JavaScript errors in DevTools

## Issue Categories

### 🔐 [Authentication Issues](/troubleshooting/authentication_issues)
Problems with login, session management, and user authentication.
- Login page not appearing
- Authentication failures
- Session and middleware issues

### 🔍 [Search Issues](/troubleshooting/search_issues)  
Problems with search functionality and indexing.
- Search results not appearing
- Search index not found
- Pagefind configuration issues

### 🎨 [Styling Issues](/troubleshooting/styling_issues)
CSS and styling-related problems.
- Styles not loading
- Layout broken
- Custom styles not applying

### 🏗️ [Build Failures](/troubleshooting/build_failures)
Build process and compilation issues.
- Build process fails
- TypeScript errors
- Missing dependencies

### 🧭 [Navigation Issues](/troubleshooting/navigation_issues)
Navigation and menu problems.
- Navigation not updating
- Pages not appearing in menu
- Wrong page order

### ⚡ [Performance Issues](/troubleshooting/performance_issues)
Performance optimization and slow loading issues.
- Slow page loads
- Large bundle sizes
- Poor search performance

### 🚀 [Deployment Issues](/troubleshooting/deployment_issues)
Deployment and production environment problems.
- Deployment failures
- Environment variables not working in production
- Site accessibility issues

### 🐳 [Docker Issues](/troubleshooting/docker_troubleshooting)
Docker-specific connectivity and setup issues.
- Docker pipe connection failures
- Container startup problems
- Development environment setup

### 🛠️ [Debugging & Help](/troubleshooting/debugging_help)
Advanced debugging techniques and getting help.
- Debug mode setup
- Browser developer tools
- Community resources and bug reporting

## Most Common Issues

### Server Won't Start
```bash
# Try these commands in order:
pnpm install
pnpm dev
```

### Environment Variables Not Working
```bash
# Check your .env.local file exists and contains:
SESSION_SECRET=your-secret-key
AUTH_USERNAME=your-username
AUTH_PASSWORD=your-password
```

### Search Not Working
```bash
# Rebuild the search index:
pnpm build
```

### Styles Not Loading
- Check if CSS files are properly imported in `app/layout.jsx`
- Clear browser cache (Ctrl+F5 or Cmd+Shift+R)
- Restart development server

## Emergency Fixes

If you're experiencing multiple issues:

<Tabs items={['Complete Reset', 'Clean Install', 'Environment Check']}>
  <Tabs.Tab>
    ```bash
    # Nuclear option - fresh start
    rm -rf node_modules .next
    rm package-lock.json # or pnpm-lock.yaml
    npm install # or pnpm install
    npm run build
    npm run dev
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    # Clean dependency installation
    rm -rf node_modules
    rm package-lock.json
    npm install
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    # Check environment setup
    node --version  # Should be 18.x or higher
    npm --version   # Should be recent
    # Check if .env.local exists with required variables
    ```
  </Tabs.Tab>
</Tabs>

## Getting Help

If you can't find a solution in the specific troubleshooting sections:

1. **Check the [Debugging & Help](/troubleshooting/debugging_help)** section for advanced techniques
2. **Search existing [GitHub Issues](https://github.com/we-digital/nextra-docs-starter-plus/issues)**
3. **Create a new issue** with detailed information about your problem

<Callout type="info">
  Most issues fall into one of the categories above. Click on the relevant section to find detailed solutions for your specific problem.
</Callout>

## Quick Reference

| Issue Type | First Thing to Try | Common Fix |
|------------|-------------------|------------|
| Won't start | `pnpm install` | Check Node.js version |
| Auth issues | Check `.env.local` | Restart server |
| Search broken | `pnpm build` | Clear cache |
| Styles wrong | Hard refresh | Check imports |
| Build fails | Clear `.next` | Update dependencies |
| Navigation | Restart server | Check `_meta.js` | 